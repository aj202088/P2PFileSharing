# Equivalent to main, name specified in project doc
from peer import Peer
from protocol import *

# Insert parsing methods here

# Create peer objects

# Start P2P connection process
def main():
    peers = []
    # Get peer info from parsing
    for peer in peers:
        new_peer = Peer(peer)

    build_handshake(peers[0])
